-[] install nvidia container toolkit to be able to use stonefish in the docker


ros-jazzy-pid-controller
ros-jazzy-joint-state-broadcaster


ideas slides

primero hablar del TAM, luego body velocity controller, con keyboard control, luego PID chain para asegurar velocidad estable

first control test, keep zero velocities


state means output reading, reference means input command