auv_velocity_controller:
  num_thrusters: {
    type: int,
    default_value: 0,
    read_only: true,
    description: "Number of thruster for robot control",
  }
  body_velocity_state_topic: {
    type: string,
    default_value: "",
    read_only: true,
    description: "Topic for body velocity feedback"
  }


  